# Permission management real collapse fix

- Replaced the currently visible "Your current access" permission renderer with closed-by-default collapsible scope cards.
- Each scope card has granted and not-granted sections with fixed-width rows and clear status labels.
- Grant/revoke permission scope cards are now collapsed by default instead of opening the first group automatically.
